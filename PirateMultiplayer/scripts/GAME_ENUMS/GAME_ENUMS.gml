enum TEAM{
	RED,
	BLUE,
	GREEN,
	YELLOW,
	WHITE,
	BLACK,
}

enum SHIP {
	SMALL,
	LARGE
}

enum DIR {
	FORWARD, //Front of ship
	STARBOARD, //Right of ship
	STERN, //Back of ship
	PORT //Left of ship
}